package main;


/**
 * This is the main game class. The function {@link Game#Game.runGame() runGame()} takes commands from the user repeatedly until the game ends.
 *
 * @author Robby
 * @version (a version number or a date)
 */
public class Game {
    /**
     * Constructor for objects of class Game
     */
    public Game() {
        // Game
    }

    public boolean runGame () {
        return true;
    }
}
